import { prisma } from "../../src/generated/prisma.ts";

export class ProductSeeder {
  async seed() {
    // await prisma.createProduct({
    //   name: "eFlat S",
    //   type: "PERK"
    // });
  }
}
