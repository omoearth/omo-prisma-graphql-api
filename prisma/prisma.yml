endpoint: https://eu1.prisma.sh/omo/omo-prisma/dev
datamodel: datamodel.graphql
secret: ${env:PRISMA_SECRET}
# generate:
#   - generator: javascript-client
#     output: ../src/generated/

# hooks:
#   post-deploy:
#     - prisma generate
